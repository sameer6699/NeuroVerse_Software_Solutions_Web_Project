# Allow build scripts for required dependencies
enable-pre-post-scripts=true
# Allow scripts from specific packages needed for build
public-hoist-pattern[]=*esbuild*
public-hoist-pattern[]=*@tailwindcss/oxide*
# Enable hoisting for better compatibility with build tools
shamefully-hoist=true
# Auto install peers
auto-install-peers=true

